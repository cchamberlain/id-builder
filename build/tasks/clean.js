// Generated by LiveScript 1.3.1
var fs, rimraf, async, map;
fs = require('fs');
rimraf = require('rimraf');
async = require('async');
map = require("prelude-ls").map;
module.exports = function(path, cb){
  fs.readdir(path, function(error, nodes){
    var paths;
    if (error) {
      return cb(error);
    }
    paths = map(function(it){
      return path + "/" + it;
    }, nodes);
    async.each(paths, rimraf, function(error){
      if (error) {
        return cb(error);
      }
      cb(null);
    });
  });
};