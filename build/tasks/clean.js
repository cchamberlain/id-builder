// Generated by LiveScript 1.3.1
var fs, map, rmrf, async;
fs = require("fs");
map = require("prelude-ls").map;
rmrf = require("rimraf");
async = require("async");
module.exports = function(path, cb){
  fs.readdir(path, function(error, nodes){
    var paths;
    if (error) {
      return cb(error);
    }
    paths = map(function(it){
      return path + "/" + it;
    }, nodes);
    async.each(paths, rmrf, function(error){
      if (error) {
        return cb(error);
      }
      cb(null);
    });
  });
};