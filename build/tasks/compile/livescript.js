// Generated by LiveScript 1.3.1
var LiveScript, file, compileChunk, compileFile, compileAllFiles, out$ = typeof exports != 'undefined' && exports || this;
LiveScript = require('LiveScript');
file = require("../../lib/file");
out$.compileChunk = compileChunk = function(chunk, cb){
  var error;
  try {
    cb(null, LiveScript.compile(chunk, {
      bare: true
    }));
  } catch (e$) {
    error = e$;
    return cb(error);
  }
};
out$.compileFile = compileFile = file.compileFile(compileChunk);
out$.compileAllFiles = compileAllFiles = file.compileAllFiles(compileFile, "ls", "js");