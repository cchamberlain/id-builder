// Generated by LiveScript 1.3.1
var async, preludeLs, jade, each, map, dependencies, run, out$ = typeof exports != 'undefined' && exports || this;
async = require('async');
preludeLs = require('prelude-ls');
jade = require("../lib/jade");
each = preludeLs.each, map = preludeLs.map;
out$.dependencies = dependencies = ["clean"];
out$.run = run = function(options, cb){
  jade.compileAllFiles(options, function(error){
    if (error) {
      return cb(error);
    }
    cb();
  });
};