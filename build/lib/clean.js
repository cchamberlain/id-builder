// Generated by LiveScript 1.3.1
var async, fs, idDebug, preludeLs, rimraf, logging, debug, error, info, warning, each, map, directory, out$ = typeof exports != 'undefined' && exports || this;
async = require('async');
fs = require('fs');
idDebug = require('id-debug');
preludeLs = require('prelude-ls');
rimraf = require('rimraf');
logging = require("./logging");
debug = idDebug.debug, error = idDebug.error, info = idDebug.info, warning = idDebug.warning;
each = preludeLs.each, map = preludeLs.map;
out$.directory = directory = function(options, cb){
  fs.readdir(options.path, function(error, nodes){
    var paths;
    if (error) {
      return cb(error);
    }
    paths = map(function(it){
      return options.path + "/" + it;
    }, nodes);
    each(function(it){
      return logging.taskInfo(options.taskName, "`" + it + "`");
    }, paths);
    async.each(paths, rimraf, function(error){
      if (error) {
        return cb(error);
      }
      cb(null);
    });
  });
};